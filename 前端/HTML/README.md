# HTML

> 超文本标记语言，Web 页面结构的基础。

## 学习要点
- 语义化标签：`<header>` / `<nav>` / `<main>` / `<article>` / `<footer>`。
- 表单与控件：`<input>` 类型、`label` 关联、HTML5 校验。
- 媒体嵌入：`<img>` / `<video>` / `<audio>` 的属性与可访问性。
- SEO 基础：标题层级、`meta` 描述、结构化数据。

## 推荐资料
- MDN HTML 文档
- HTML5 官方规范
